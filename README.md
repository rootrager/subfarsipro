![SubFarsiPro v5.0 Poster](assets/poster.png)
# 🎬 SubFarsiPro v5.0 | Nexplore Studio Edition

سلام! به **SubFarsiPro v5.0** خوش اومدید. 
این ابزار، یک دستیار حرفه‌ای، پرسرعت و متن‌باز برای استخراج زیرنویس از ویدیوها و ترجمه دقیق اون‌ها به **زبان فارسی (با لحن طبیعی و محاوره‌ای)** هست. 

توی این نسخه (Nexplore Studio)، ما با اسکریپت‌های خشک خداحافظی کردیم و همه‌چیز رو از نو ساختیم؛ از یک رابط کاربری دارک و فوق‌العاده جذاب گرفته تا یک موتور پردازشی چندهسته‌ای که تو کمترین زمان، بهترین خروجی رو بهتون میده.

---

## 🔥 ویژگی‌های نسخه ۵.۰

* **محیط کاربری استودیویی:** دیزاین سه‌ستونه، مینیمال و شیشه‌ای با نمایش خودکار کاور ویدیو (توسط OpenCV).
* **موتور پردازشیِ موازی:** استفاده از نهایت توان CPU/GPU (Multithreading) بدون هنگ کردن رابط کاربری.
* **ترجمه روان و بدون توهم:** خروجی دقیقاً شبیه لحن یک تولیدکننده محتوای تهرانی است، بدون کلمات رباتیک و عجیب.
* **آزادی انتخاب:** پشتیبانی از مدل‌های لوکال (کاملاً آفلاین) و APIهای ابری (Gemini، Groq و...).

---

## 📦 راهنمای نصب و اجرای نسخه‌های آماده (پیشنهادی)

برای استفاده از برنامه نیازی به نصب پایتون و درگیری با کدها ندارید! ما نسخه‌های آماده و کامپایل‌شده را برای شما آماده کرده‌ایم. با توجه به اینکه فایل‌ها بهینه‌سازی شده‌اند (شامل تمامی پیش‌نیازها در یک پوشه)، لطفاً بر اساس سیستم‌عامل خود مراحل زیر را دنبال کنید:

### 🪟 راهنمای ویندوز (Windows)

1. فایل `SubFarsiPro-Windows-executable.zip` را دانلود کنید.
2. فایل را به طور کامل از حالت فشرده خارج کنید (Extract).
3. وارد پوشه `SubFarsiPro` شده و روی فایل اجرایی `SubFarsiPro.exe` دو بار کلیک کنید.

> [!WARNING]
> **نکته مهم:** از آنجا که این برنامه متن‌باز و بدون گواهی‌نامه دیجیتال (Unsigned) است، ممکن است ویندوز **SmartScreen** یک صفحه هشدار آبی‌رنگ نشان دهد. در این صورت روی **More info** (اطلاعات بیشتر) کلیک کرده و سپس **Run anyway** (اجرا در هر صورت) را انتخاب کنید.

### 🐧 راهنمای لینوکس (Linux)

1. فایل `SubFarsiPro-Linux-executable.zip` را دانلود کنید.
2. فایل فشرده را Extract کنید.
3. ترمینال را در داخل پوشه استخراج شده باز کنید و با دستور زیر به فایل، اجازه اجرا بدهید:
   ```bash
   chmod +x SubFarsiPro
   ```
4. حالا برنامه را از طریق ترمینال با دستور `./SubFarsiPro` اجرا کنید (یا روی آن دو بار کلیک کنید).

### 🍎 راهنمای مک (macOS)

1. فایل `SubFarsiPro-macOS-executable.zip` را دانلود کنید.
2. فایل فشرده را Extract کنید.
3. فایل اجرایی `SubFarsiPro` را در داخل پوشه پیدا کنید.

> [!IMPORTANT]
> **نکته مهم:** به دلیل سیستم امنیتی **Gatekeeper** در سیستم‌عامل مک، در صورت دوبار کلیک ممکن است خطای "unidentified developer" بگیرید. برای دور زدن دائمی این محدودیت:
> روی فایل **کلیک راست** (Right-Click) کرده، گزینه **Open** (باز کردن) را انتخاب کنید و سپس در کادر باز شده مجدداً روی **Open** کلیک کنید.

---

## 🚀 راهنمای اجرای سورس کد (مخصوص توسعه‌دهندگان)

برای اجرای برنامه، فقط کافیه این ۳ قدم رو به ترتیب انجام بدید تا هیچ اروری نگیرید:

### قدم اول: نصب پیش‌نیازهای پایتون
مطمئن بشید که پایتون (نسخه ۳.۸ به بالا) روی سیستمتون نصبه. ترمینال رو تو پوشه پروژه باز کنید و دستور زیر رو بزنید تا تمام کتابخونه‌های لازم (مثل CustomTkinter و OpenCV) نصب بشن:

```bash
pip install -r requirements.txt
```

### قدم دوم: نصب موتور ترجمه آفلاین (Ollama) و مدل پیشنهادی

ما برای ترجمه، استفاده از Ollama رو پیشنهاد می‌دیم چون کاملاً رایگان، امن و آفلاینه.

۱. اول برنامه Ollama رو دانلود و نصب کنید.
۲. مدل پیشنهادی ما: ما به شدت مدل translategemma رو بهتون پیشنهاد می‌دیم. این مدلِ فوق‌العاده که بر پایه Gemma 3 ساخته شده (در نسخه‌های 4B، 12B و 27B در دسترسه)، یک قدم رو به جلو و بزرگ در دنیای ترجمه متن‌بازه. این مدل فوق‌العاده سبک، پرسرعت و برای ترجمه بین ۵۵ زبان بی‌رقیبه.
برای دانلود این مدل، ترمینال رو باز کنید و بزنید:

```bash
ollama run translategemma
```

(نکته بسیار مهم: قبل از اجرای برنامه SubFarsiPro، همیشه مطمئن بشید که نرم‌افزار Ollama تو پس‌زمینه سیستم شما در حال اجراست!)

### قدم سوم: اجرای استودیو

حالا که همه‌چیز آماده‌ست، این دستور رو بزنید تا برنامه باز بشه:

```bash
python main_app.py
```

### ⚙️ راهنمای بخش تنظیمات (AI Configuration)

وقتی برنامه باز شد، تو پنل سمت راست (AI Configuration) می‌تونید همه‌چیز رو شخصی‌سازی کنید:

* **Whisper Model (مدل رونویسی صدا):** اینجا می‌تونید دقت و سرعت استخراج صدای انگلیسی رو تعیین کنید. مدل base سرعت فوق‌العاده‌ای داره و برای سیستم‌های معمولی عالیه. اگه سیستم قوی‌تری دارید می‌تونید روی small یا medium بذارید.
* **Translation Provider (موتور ترجمه):** اگه می‌خواید آفلاین کار کنید، این رو روی ollama بذارید. اگه کلید API دارید، می‌تونید سرویس‌های ابری رو انتخاب کنید.
* **Model (نام دقیق مدل):** در این کادر باید اسم مدلی که دانلود کردید رو بنویسید (مثلاً translategemma یا translategemma:latest).
* **Target Language (زبان مقصد):** روی همون Persian (Informal Tehrani) تنظیم شده تا خروجی محاوره‌ای و جذابی بهتون بده.
* **Batch Size (حجم پردازش گروهی):** این عدد نشون میده که تو هر مرحله، چند خط زیرنویس برای ترجمه ارسال بشه. بهترین و پایدارترین حالت بین ۴ تا ۸ هست. اگه سیستمتون خیلی قویه می‌تونید این عدد رو بالاتر ببرید.

## 🛡️ امنیت و حریم خصوصی

* هیچ مسیر شخصی یا هاردکدی تو کدها وجود نداره.
* کلیدهای API شما کاملاً امن و فقط روی سیستم خودتون ذخیره می‌شن.
* با استفاده از ترکیب Ollama + Whisper، هیچ دیتایی از ویدیوی شما وارد اینترنت نمیشه.

Developed with ❤️ by the Nexplore Tech Media.

SubFarsiPro v5.0 is an open-source, heavily optimized AI tool designed to transcribe and translate video subtitles into natural Persian (Informal Tehrani dialect). It features a modern 3-column GUI, background multithreading, OpenCV thumbnail extraction, and supports models like translategemma (Gemma 3) via Ollama for high-speed, zero-hallucination processing.
Install requirements: pip install -r requirements.txt, run Ollama, and start with python main_app.py.